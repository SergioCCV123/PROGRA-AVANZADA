@model IEnumerable<ProyectoPulperia.Historial>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Compra.USERID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AspNetUsers.Email)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Compra.USERID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AspNetUsers.Email)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDHISTORIAL }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDHISTORIAL }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDHISTORIAL })
        </td>
    </tr>
}

</table>
